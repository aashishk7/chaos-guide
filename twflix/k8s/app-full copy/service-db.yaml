---

apiVersion: v1
kind: Service
metadata:
  name: twflix-demo-db
  labels:
    app: twflix-demo-db
    release: primary
spec:
  type: ClusterIP
  ports:
  - name: mongodb
    port: 27017
    targetPort: mongodb
  selector:
    app: twflix-demo-db
    release: primary
