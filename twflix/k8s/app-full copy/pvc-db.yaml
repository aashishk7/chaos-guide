---

kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  labels:
    app: twflix-demo-db
    release: primary
  name: twflix-demo-db
spec:
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: "8Gi"
